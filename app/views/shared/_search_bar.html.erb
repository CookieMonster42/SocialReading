<div class="justify-content-center">
  <div class="row ">
    <div class="col-lg-12">
      <%= simple_form_for :search, url: groups_path, method: :get do |f| %>
        <div class="search-form-control form-group px-4">
          <!-- <input class="form-control string required" type="text" name="search[query]" id="search_query" /> -->
          <%= text_field_tag :query, params[:query], class: "form-control col-lg-10", placeholder: "Where do you want to meet?" %>
          <button name="button" type="submit" class="btn btn-regular pl-3">
            <i class="fas fa-search"></i> <%#= "#{content}" %>
          </button>
        </div>
      <% end %>
    </div>
  </div>
</div>


<div class="d-flex col-lg-3 m-auto flex-column">
        <%#= text_field_tag :query, params[:query], class: "col-lg-10", placeholder: "Where do you want to meet?" %>
        <div class="line"></div>
        <div class="mt-2 m-auto">
          <%= render 'shared/button_regular', content: "Search" %>
        </div>
      </div>
